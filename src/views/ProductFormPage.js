import ProductForm from "../components/ProductForm";
import PageTemplate from "./PageTemplate";

const ProductFormPage = () => {
  return (
    <PageTemplate title="Product Create Form">
      <ProductForm />
    </PageTemplate>
  );
};

export default ProductFormPage;
